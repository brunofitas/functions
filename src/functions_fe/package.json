{
  "name": "functions-fe",
  "version": "0.0.1",
  "private": true,
  "description": "functions — Studio GUI (run console, pipeline editor, browser, settings)",
  "license": "MIT",
  "author": "Bruno Fitas <brunofitas@gmail.com>",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "build": "esbuild src/studio.ts --bundle --format=iife --outfile=dist/studio.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --coverage",
    "app": "electron ."
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "esbuild": "^0.23.0",
    "electron": "^31.0.0"
  }
}
